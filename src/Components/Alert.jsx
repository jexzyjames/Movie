import React from "react";
import "../styles/Details.css";

const Alert = () => {
  console.log("from the error component");
  return (
    <div>
      <h1>A very bad error A very bad error A very bad error</h1>
    </div>
  );
};

export default Alert;
